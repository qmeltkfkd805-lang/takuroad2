# TAKUROAD 작업 인수인계 — 2026-09-10

새 대화에서 이 문서를 첨부하고 "이어서 진행해줘" 라고 하면 그대로 이어집니다.

지금 **두 갈래 작업**이 있습니다.

- **A. EXP·보상 서버 이전** — 일시 중단 상태. 안전한 지점에서 멈춰 있음
- **B. Supabase Storage 용량 초과 정리** — 진행 중. 지금 여기가 우선

---

# 0. 작업 방식 (이걸 먼저 읽어주세요)

이 프로젝트에서 지켜온 규칙입니다. 새 대화에서도 그대로 유지해주세요.

## 진행 방식

- **한 메시지에 한 단계씩.** 복사해서 바로 붙여넣을 수 있는 블록을 주고, 결과를 받은 뒤 다음 단계로 간다. 여러 단계를 한 번에 쏟아내지 않는다.
- **SQL 은 채팅에 전문을 붙여넣는다.** "파일에 있으니 열어보세요" 로 넘기지 않는다. 파일에도 저장하되 채팅에도 실행할 SQL 을 그대로 준다.
- **롤백 SQL 을 적용 SQL 과 같은 메시지에 넣지 않는다.** 과거에 사용자가 실수로 롤백 블록을 실행한 적이 있다. 롤백은 필요해질 때 따로 준다.
- **추측 금지.** 테이블·컬럼 이름을 이름만 보고 짐작하지 말고 실제로 확인한다. 확인 못 하면 "확인 불가"라고 말한다.
- **적용 전 보고 → 사용자 승인 → 적용.** DB 변경, 권한 회수, 사용자 화면 변경 전에는 반드시 설계를 먼저 보고한다.
- **화면이 깨졌을 때 DB 를 먼저 의심하지 않는다.** 과거 두 번의 사고가 모두 DB 와 무관했다 (prop 파생 state, 3000 포트 좀비 프로세스). 근거를 모으고 나서 판단한다.

## 파일 전달

- 사용자는 **다운로드 후 Copy-Item 방식을 싫어한다.** 
- 이번 세션에서는 `mcp__remote-devices__device_commit_files` 로 **저장소에 직접 써 넣는 방식**을 썼고 잘 통했다. 이 방식을 유지할 것.
  - 순서: 파일 작성 → `SendUserFile` 로 보내 `file_uuid` 획득 → `device_commit_files` 로 `C:\dev\takuroad\...` 에 기록
- PowerShell 로 줄 때는 here-string(`@'...'@ | Set-Content -Encoding UTF8`) 또는 ReadAllText/.Replace/WriteAllText 방식.
- migration SQL 은 전부 `C:\dev\takuroad\migrations\` 에 넣는다.

## 코드·디자인 규칙

- 사용자 사이트는 승인 없이 수정하지 않는다. 관리자 화면은 상대적으로 자유롭다.
- 기존 데이터 구조·서비스·권한·탭 전환 방식과 동작은 유지한다.
- 시안의 숫자를 하드코딩하지 않는다. 기능 없는 장식용 버튼·필터를 만들지 않는다.
- **이모지 사용 금지.** 핑크는 활성 메뉴와 주요 등록 버튼에만.
- `AGENTS.md` 와 `node_modules/next/dist/docs/` 를 먼저 확인한다.
- `react-hooks/set-state-in-effect` — effect 본문에서 동기 `setState` 금지. 키 기반 파생 로딩, 렌더 중 prop 동기화 등으로 우회한다.
- 새 `any` 를 만들지 않는다. 기존 `any` 는 `Database = any` 때문이며 후속 백로그 항목이다.
- `npm run dev` / `npm run build` 를 쓴다. `npx next build` 는 Turbopack 이라 webpack dev 용 `.next` 를 깨뜨린다.

## 커밋

단계별로 분리해서 커밋한다. 메시지는 한국어로 "무엇을 왜" 를 적고 끝에 아래를 붙인다.

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01UWXjE2rpzoGLTxq4Hb5Dap
```

## 환경

- 저장소 `C:\dev\takuroad`, Windows + PowerShell 5.1
- Next.js 16.2.9 App Router (webpack)
- Supabase (SQL Editor 에서 사용자가 직접 실행 → 결과를 붙여넣음)
- Supabase SQL Editor 는 **선택한 텍스트만 실행하고 마지막 문장 결과만 보여준다.** 여러 결과를 보려면 `union all` 로 묶고, `limit` 이 붙은 branch 를 `union` 으로 이으면 문법 오류가 난다 (괄호로 감싸야 함).

---

# A. EXP·보상 서버 이전 — ✅ 완료 (2026-09-22)

> 아래 A-1 ~ A-9 는 당시 기록이다. 단계 3~7 이 전부 적용·검증됐다.
>
> - 단계 3: 서버 API 3개 main 반영 (`8a86d8d`)
> - 단계 4a: 배지 평가 → `/api/badges/evaluate` (`eded996`)
> - 단계 4b: 활동 기록·보상 → `/api/activity` (`40a6dea`). 샵 방문·이벤트 참여 EXP 0, 참여 날짜는 서버가 원본에서 읽어 보존
> - 단계 4c: 제보 채택·팬아트 → 서버 (`9a08d94`)
> - 단계 5a: 작품 등록 관리자 전용 + 보상 중단 (`01b31dd`) · `tags_user_insert` 정책 삭제 · 대체로 작품 추가 요청 기능 신설 (`ab6e4d0`)
> - 단계 5b: 루트 완주 기록·보상 분리 (`6c2afd1`). 비GPS 완주는 기록만(EXP 0), GPS 완주는 모든 샵이 현장 확인된 경우만 15 EXP·배지. `mode:complete` 는 완료 증거가 아님
> - M3: 검증 불가 배지 15종 비활성화 (보유 행 회수 없음)
> - M4: `activity_logs`·`user_badge_tiers` 쓰기와 `grant_exp` 실행을 브라우저에서 회수. 일반 사용자 JWT 위조 테스트 3종 42501 차단

> 남은 것: 죽은 코드(`createActivity`·`logActivity`·`setFeaturedBadgeTiers`·`awardTierManually`)는 주석으로 표시만 해둠. B 섹션(Storage 정리)은 별도 진행 완료.


## A-1. 왜 하는 작업인가

`grant_exp` 가 `PUBLIC`·`anon` 에 열려 있어서 비로그인 포함 누구나 아무에게나 EXP 를 무한 지급·차감할 수 있었다. 1차로 호출자 가드를 넣어 막았고(적용 완료), 그 다음 단계로 **보상 로직 자체를 브라우저에서 서버로 옮기는** 작업을 하던 중이었다.

조사 과정에서 더 근본적인 문제가 드러났다.

- `activity_logs` 를 브라우저가 직접 INSERT 한다. 배지 조건이 이 테이블을 읽으므로 **배지를 그대로 farm 할 수 있다.**
- `check_ins`·`event_visits`·`route_completions` 는 현장 증명이 없다. 버튼을 누르면 생기는 자기 주장이라 서버가 대조해도 "눌렀다"까지만 확인된다.

그래서 방향이 이렇게 정해졌다.

- **검증 가능한 콘텐츠 활동** (review, photo_upload, shop_register, route_created, fanart) → 서버가 원본 행을 대조한 뒤 기록·지급
- **작품 등록 (work_register)** → 검수 정책 전까지 보상 중단, 등록 기능도 UI 에서 막음
- **방문·이벤트 참여** → 기록은 남기되 **EXP 지급 중단**, 관련 배지 비활성화
- **루트 완주** → GPS 세션(`/api/route-session/end`)이 검증한 것만 보상. 비GPS 경로는 완주 행만 저장
- **기존 EXP·배지·activity_logs 는 회수·삭제하지 않는다**

## A-2. 적용 완료 (롤백하지 말 것)

| migration | 상태 | 내용 |
|---|---|---|
| `rpc_caller_guards.sql` | **적용** | security definer RPC 호출자 검증. `grant_exp`·`claim_daily_*`·`cast_poll_vote`·`increment_route_likes`·`get_dashboard_stats` 등 10개 함수의 anon·PUBLIC EXECUTE 회수 + 내부 가드 |
| `tags_write_policies.sql` | **적용** | `tags_authenticated_update using(true)` 제거 (로그인한 누구나 작품 1,992건 전체를 수정할 수 있었음). INSERT·소유자 UPDATE 를 `created_by = auth.uid()` 로 강제 |
| `activity_reward_1_schema.sql` | **적용** | `activity_logs.source_id`, `uq_activity_logs_source`, `uq_exp_logs_once`, `route_completions.verification_source` + 컬럼 권한 |
| `activity_reward_2_rpc.sql` | **적용** | `grant_exp`(on conflict 기반 멱등), `record_activity_reward`, `record_gps_route_completion` |

**1·2 는 적용됐지만 동작을 바꾸지 않는다.** 새 컬럼·인덱스를 아무도 안 쓰고, 새 RPC 는 호출부가 없다. 그대로 두면 안전하다.

## A-3. 미적용 (절대 지금 적용하지 말 것)

| migration | 내용 |
|---|---|
| `activity_reward_3_badges.sql` | 검증 불가 활동 기반 배지 15종 `is_active=false` |
| `activity_reward_4_lockdown.sql` | `activity_logs`·`grant_exp`·`user_badge_tiers` 권한 회수 |

## A-4. 만들었지만 아직 아무도 호출하지 않는 파일

```
src/app/api/activity/route.ts                  (신규)
src/app/api/badges/evaluate/route.ts           (신규)
src/app/api/admin/approve-submission/route.ts  (신규)
```

- `/api/activity` — 본문은 `{ type, sourceId }` 만 허용, 다른 필드는 400. userId 는 세션, 금액·related_id·title 은 RPC, 스냅샷은 라우트가 원본 행을 읽어 생성
- `/api/badges/evaluate` — 본문 없음, 세션 사용자 고정
- `/api/admin/approve-submission` — 서버 admin 확인, 제보자·상태를 DB 재조회, 재호출 시 이벤트 재생성 없이 보상만 재시도

**tsc·eslint 검사를 아직 안 돌렸다.** `geekAreaFromAddr` import 경로와 `serviceClient()` 반환 타입에서 걸릴 수 있음.

## A-5. 커밋 상태

마지막 커밋: `6dee125c881521396a2c54b33c396caf95d61ddd`
> fix: 서버 배지 재평가에서 보너스 EXP 가 빠지던 회귀 수정

**커밋 안 된 파일** (사용자 지시로 커밋 보류):
```
migrations/tags_write_policies.sql
migrations/activity_reward_1_schema.sql
migrations/activity_reward_2_rpc.sql
migrations/activity_reward_3_badges.sql
migrations/activity_reward_4_lockdown.sql
src/app/api/activity/route.ts
src/app/api/badges/evaluate/route.ts
src/app/api/admin/approve-submission/route.ts
```

## A-6. 재개할 때 다음 단계

1. 위 3개 API 의 `npx tsc --noEmit` + eslint 통과 확인
2. 커밋 (단계 3: "신규 서버 API")
3. 호출부 교체 (단계 4) — `activityService.createActivity` 를 `/api/activity` 호출로, `RouteMapMode.tsx:154` 와 `communityPostService.ts:88` 의 우회 호출 흡수, `AppShell`·`checkInService` 의 배지 평가를 `/api/badges/evaluate` 로, `eventSubmissionService.approveSubmission` 을 `/api/admin/approve-submission` 으로
4. 보상 중단 + UI 문구 (단계 5)
5. 배지 비활성화 (단계 6, migration 3)
6. 회귀 테스트 16항목
7. 권한 lockdown (단계 7, migration 4)

## A-7. 단계 5 에서 고쳐야 할 UI 문구

| 파일 | 변경 |
|---|---|
| `src/components/growth/GrowthPage.tsx:78-103` | "경험치 얻는 방법" 팝오버에서 샵 방문·이벤트 참여·작품 등록 제거. 루트 완주는 "GPS 루트 완주"로 명시 |
| `src/components/activity/MyActivityPage.tsx:269` | `+N EXP` 를 `XP_RULES` 계산이 아니라 **실제 `exp_logs` 기준**으로 변경 (안 그러면 새 방문에 "+5 EXP" 가 거짓 표시됨) |
| `src/services/activityService.ts:643` | `'굿즈샵 방문을 인증했어요'` → `'굿즈샵에 다녀왔어요'` (인증 표현 제거) |
| `src/services/expService.ts:50-65` | `XP_RULES` 에 `active` 플래그 추가 (표시용). 지급은 서버가 결정 |
| `/work/new` 진입점 | 일반 사용자에게 숨기고, 직접 접근 시 "작품 등록 기능을 준비 중이에요" 안내. 권한 판정은 `profiles.role` 기준 서버 확인. 관리자 진입은 유지 |

## A-8. 비활성화 대상 배지 15종 (migration 3)

`shop_visit` 기반: 첫 탐험 `f38918da`(보유 1) · 샵 탐험가 `92a85d62`(1) · 샵 마스터 `56107440`(1) · 성지순례자 `056e7ba0`(1) · 전국 탐험가 `a69d4acc`(1) · 전국 정복자 `96689cd9`(0)

`event_visit` 기반: 이벤트 첫걸음 `3ad354ae`(2) · 이벤트 러버 `f80cef8a`(0) · 이벤트 헌터 `2b50ba04`(0) · 이벤트 마스터 `7a9fec14`(0)

`work_register` 기반: 컬렉터 `6c237ed5`(2) · 타쿠컬렉터 `5eb6e9e8`(1) · 전설의 컬렉터 `8c1ef149`(0) · 수집 마스터 `bb9a4c2a`(0)

파생: 전설의 타쿠 `564eaef5`(0) — `all_masters` 조건이 크게 쉬워지므로 같이 내림

**루트 배지 3종은 유지** (루트 초보 `1b19687e`, 루트 탐험가 `16a31e73`, 루트 마스터 `d2007554`) — GPS 완주로만 진행되므로.

적용 전에 slug·조건·보유자 수를 다시 대조하고 검증 쿼리 결과를 남길 것 (사용자 지시).

## A-9. 알아둘 기술적 사실

- `security definer` 는 권한만 바꾸고 JWT 는 그대로다. definer 함수 안에서도 `auth.uid()` 는 원래 호출자를 가리킨다. 이게 `grant_exp` 가드가 안전한 이유다.
- PostgreSQL 컬럼 권한은 테이블 권한에 **더해진다.** 테이블 INSERT 가 살아 있으면 컬럼 REVOKE 는 무효다. "테이블 회수 → 컬럼 부여" 순서로 해야 한다.
- `information_schema.table_privileges` 를 `grantee in ('anon','authenticated')` 로 조회하면 **PUBLIC 에 부여된 권한이 안 잡힌다.** `has_table_privilege()` 를 써야 한다. 이것 때문에 한 번 오판했다.
- `work_progress` 는 호출 경로가 끊겨 있다 (`tagCollectionService.addTagToCollection` 을 부르는 곳이 저장소에 없음). 되살릴 때 `reason` 을 `work_progress_25` 처럼 마일스톤별로 나눠야 `uq_exp_logs_once` 와 충돌하지 않는다.
- `activity_logs.title` 은 NOT NULL 이다. `record_activity_reward` 가 스냅샷에서 제목을 만든다 (`buildLegacyTitle` 과 같은 규칙).
- 팬아트는 `activity_logs` 행을 만들지 않는다 (기존 동작 유지). EXP 만 주고 멱등은 `uq_exp_logs_once` 가 보장.

## A-10. 후속 백로그

- `shops` 권한: `owner_id` 가 authenticated INSERT 허용 컬럼에 있음. `is_claimed`·`is_verified` 는 이미 막혀 있어 "공식 샵 위조"는 불가
- `shops_update_tiered` 가 미청구 샵 전체를 아무나 수정 가능하게 함 (위키형 의도로 보임). `review_status`·`reviewed_at`·`reviewed_by` 가 UPDATE 허용 컬럼에 있고 트리거로만 막힘
- `run_fanart_selection` 의 `search_path` 가 `public` 만 (실행 권한은 postgres·service_role 뿐이라 안전)
- `getAdminStats` 가 실패 시 0을 반환
- `NOTI_ICON` 맵이 `TopBar.tsx`·`NotificationsPage.tsx` 두 곳에 중복. `exp_grant`·`level_up` 아이콘
- `contact-files` 가 공개 버킷
- `export type Database = any` (~1,280 lint 오류), `<img>` → `next/image`

---

# B. Supabase Storage 정리 (현재 진행 중)

## B-1. 문제

전체 **1.41 GiB** (1,517,188,643 bytes). 무료 한도 1 GB 초과.

| 버킷 | 파일수 | 용량 |
|---|---|---|
| shop-images | 273 | 1,050 MB |
| event-goods | 411 | 370 MB |
| post-images | 48 | 14 MB |
| verify-documents | 10 | 5.3 MB |
| review-images | 6 | 4.4 MB |
| places | 4 | 2.9 MB |
| exhibit-images | 5 | 192 KB |
| avatars | 3 | 71 KB |
| contact-files | 1 | 36 KB |
| goods-images | 0 | 0 |

## B-2. 근본 원인 두 가지

**하나 — 샵 사진이 원본 해상도 그대로 올라간다.** `shopService.ts:952-965` 가 `File` 을 그대로 `upload()` 에 넘긴다. TOP 50 이 전부 `{샵slug}/main/*.jpg` 이고 장당 7~9 MB.

**둘 — 교체·삭제 시 이전 파일을 지우는 코드가 없다.** 특히 `PhotosManage.tsx:257-274` 의 크롭 저장은 새 파일을 올리고 DB 행만 지운다. 크롭할 때마다 7~9 MB 가 쌓인다.

정리 로직이 있는 버킷은 `avatars`·`goods-images`·`exhibit-images` 셋뿐. 기존 크론 `/api/exhibit/cleanup` 은 `exhibit-images` 만 화이트리스트에 있고 큐에 적재된 것만 지운다. **DB 참조가 끊긴 파일을 스캔하는 코드는 없다.**

## B-3. 버킷별 코드 지도

| 버킷 | 업로드 경로 | 저장 컬럼 | 저장 형태 | 삭제 코드 | 교체 시 구파일 |
|---|---|---|---|---|---|
| `shop-images` 샵사진 | `{slug}/main/{ts}.{ext}` | `shop_images.image_url` | 풀 public URL | 없음 | 잔류 |
| `shop-images` 매장소식 | `{slug}/events/{ts}.{ext}`, `video-{ts}` (최대 50MB) | `shop_events.image_url`/`video_url` | 풀 URL | 없음 | 잔류 |
| `shop-images` 배너·홈히어로 | `banners/{ts}.{ext}` | `featured_banners.image_url`, `home_hero_slots.custom_image_url` | 풀 URL | 없음 | 잔류 |
| `shop-images` 작품 | `works/{slug}/{kind}/{ts}.{ext}` | `tags.cover_url`, `tags.banner_image` | 풀 URL | 없음 | 잔류 |
| `shop-images` 커뮤니티 | `community/{userId}/{ts}-{rand}.{ext}` | `community_posts.images` (jsonb) | 풀 URL | 실패 롤백만 | 잔류 |
| `shop-images` 이의제기 | `community-appeal/{userId}/...` | `post_appeals.proof_images` | 풀 URL | 없음 | 잔류 |
| `shop-images` 루트커버 | `routes/{userId}/{routeKey}/{ts}.{ext}` | `routes.cover_image_url` | 풀 URL | 없음 | 잔류 |
| `shop-images` 하이라이트 | `{slug}/highlights/{ts}.{ext}` | `shop_highlights.image_url` | 풀 URL | 없음 | 잔류 |
| `shop-images` 공지 | `notices/{ts}.{ext}` | `notices.image_url` | 풀 URL | 없음 | 잔류 |
| `shop-images` 레거시아바타 | `avatars/{userId}-{ts}.{ext}` | `profiles.avatar_url` | 풀 URL | 없음 | 잔류 |
| `avatars` | `{userId}/{uuid}.webp` | `profiles.avatar_url` | 풀 URL | **있음** | **삭제함** |
| `goods-images` | `{userId}/{uuid}.webp` | `goods_item_images.bucket_name` + `object_path` | **경로만** | 있음 | 해당 없음 |
| `exhibit-images` | `{userId}/{exhibitId}/{uuid}.{ext}` | `exhibit_images.object_path` | 경로만 | 있음(큐) | 해당 없음 |
| `event-goods` | `{eventId}/{rand}.{ext}`, `covers/{rand}.{ext}` | `event_goods.image_url`, `events.cover_url` | 풀 URL | 없음 | 잔류 |
| `verify-documents` | `{userId}/{shopId}-{ts}.{ext}` | `shop_verify_requests.evidence_url` | **경로만** | 없음 | 잔류 |
| `review-images` | `{reviewId}/{i}.{ext}` (upsert) | `review_images.image_url` | 풀 URL | 없음 | 확장자 바뀌면 잔류 |
| `places` | `covers/{rand}.{ext}` | `places.cover_image` | 풀 URL | 없음 | 잔류 |
| `contact-files` | `contact/{ts}-{rand}.{ext}` | `contact_messages.attachment_urls` (ARRAY) | 풀 URL | 없음 | 잔류 |
| `post-images` | **코드에서 사용처 없음** | ? | ? | — | — |

`migrations/` 에는 storage 관련 SQL 이 하나도 없다. 버킷·정책·정리 큐 트리거가 저장소에 없고 대시보드에서 직접 관리된 것으로 보인다.

## B-4. 1차 조사 결과 (suffix LIKE 기준 — 이 숫자는 참고용)

| 버킷 | 고아수 | 고아용량 |
|---|---|---|
| shop-images | 114 | 489 MiB |
| post-images | 48 | 14 MiB |
| event-goods | 12 | 4.8 MiB |
| review-images | 6 | 4.3 MiB |
| verify-documents | 7 | 1.8 MiB |
| places | 1 | 1.3 MiB |
| avatars | 2 | 49 KB |
| **합계** | **190** | **약 515 MiB** |

shop-images 고아를 폴더별로 보면 `{샵}/main/` 85개가 473 MiB 로 대부분이다.

검증으로 확인된 것:
- `review_images` 테이블 행수가 **0** → review-images 6개는 진짜 고아
- `avatars` 2개는 정리 로직 생기기 전의 `.jpg`
- `places` 1개는 바이트 동일 중복 2장 중 미참조분
- `community/` 는 `goods_item_images` 를 참조에 넣자 12개 → **6개로 줄었다.** 그대로 지웠으면 굿즈 사진 6장이 깨졌을 것

## B-5. 사용자가 내린 결정

1. 일반 고아 파일은 **검증과 로컬 백업 후** 삭제
2. `dopaminseuteisyeon-yongsan-aipakeumol` 의 2장은 **삭제 제외** (`shop_images` 행이 0개라 유일본일 수 있음)
3. 미참조 인증 증빙 6개는 **이번 삭제 제외**
4. 최초 삭제 대상은 최대 181개지만 재검증으로 더 줄 수 있음
5. **`like '%' || o.name` 방식 금지.** 버킷 + object path 를 파싱해 정확히 일치시킬 것
6. 파싱 불가 값은 삭제 후보에서 제외하고 별도 보고
7. `post-images` 48개도 "코드에 없다"만으로 삭제하지 말고 문자열·JSON 전수 검사할 것
8. 실제 삭제는 최종 승인 후에만

## B-6. 삭제 절차 (승인 후)

1. 매니페스트를 CSV 로 `C:\dev\takuroad-storage-cleanup\manifest-<날짜>.csv` 에 저장 (Git 에 넣지 않음)
2. service_role 로 대상 파일을 전부 다운로드해 `backup/<bucket>/<원래 경로>` 에 저장
3. 파일 수·총 바이트 대조 + 파일별 SHA-256 을 `backup-manifest.json` 에 기록. 하나라도 불일치면 삭제로 넘어가지 않음
4. `--dry-run` 이 기본인 일회성 Node 스크립트
5. 첫 삭제는 **20개만**. 화면 확인 후 나머지 진행
6. 실패 경로는 최대 2회 재시도, 그 뒤 `failed.json` 에 남기고 중단
7. 성공 응답이 온 경로만 성공 로그에 기록
8. **`storage.objects` 직접 DELETE 금지** — 메타데이터 행만 지워지고 실제 파일은 남아 용량이 안 줄어든다. Storage API (`storage.from(bucket).remove(paths)`) 만 사용
9. service_role 키는 `.env.local` 에서만 읽고 코드·CSV·로그·커밋에 남기지 않는다. 스크립트와 백업 폴더는 `.gitignore` 에 추가

## B-7. 바로 다음에 할 일 — 사용자에게 실행 요청할 쿼리 5개

아래 쿼리들을 사용자가 Supabase SQL Editor 에서 실행하고 결과를 붙여넣으면 이어서 진행한다.
전부 **조회만** 한다.

### (1) 정규화된 최종 매니페스트

```sql
with url_refs as (
            select 'shop_images.image_url'            as src, image_url        as u from public.shop_images            where image_url        is not null
  union all select 'shop_events.image_url',                    image_url             from public.shop_events            where image_url        is not null
  union all select 'shop_events.video_url',                    video_url             from public.shop_events            where video_url        is not null
  union all select 'shop_highlights.image_url',                image_url             from public.shop_highlights        where image_url        is not null
  union all select 'notices.image_url',                        image_url             from public.notices                where image_url        is not null
  union all select 'featured_banners.image_url',               image_url             from public.featured_banners       where image_url        is not null
  union all select 'home_hero_slots.custom_image_url',         custom_image_url      from public.home_hero_slots        where custom_image_url is not null
  union all select 'tags.cover_url',                           cover_url             from public.tags                   where cover_url        is not null
  union all select 'tags.banner_image',                        banner_image          from public.tags                   where banner_image     is not null
  union all select 'routes.cover_image_url',                   cover_image_url       from public.routes                 where cover_image_url  is not null
  union all select 'profiles.avatar_url',                      avatar_url            from public.profiles               where avatar_url       is not null
  union all select 'shop_product_images.image_url',            image_url             from public.shop_product_images    where image_url        is not null
  union all select 'shop_suggestions.image_url',               image_url             from public.shop_suggestions       where image_url        is not null
  union all select 'fan_arts.image_url',                       image_url             from public.fan_arts               where image_url        is not null
  union all select 'seasonal_events.cover_image_url',          cover_image_url       from public.seasonal_events        where cover_image_url  is not null
  union all select 'events.cover_url',                         cover_url             from public.events                 where cover_url        is not null
  union all select 'events.video_url',                         video_url             from public.events                 where video_url        is not null
  union all select 'event_goods.image_url',                    image_url             from public.event_goods            where image_url        is not null
  union all select 'event_goods_history.image_url',            image_url             from public.event_goods_history    where image_url        is not null
  union all select 'places.cover_image',                       cover_image           from public.places                 where cover_image      is not null
  union all select 'places.cover_url',                         cover_url             from public.places                 where cover_url        is not null
  union all select 'review_images.image_url',                  image_url             from public.review_images          where image_url        is not null
  union all select 'goods_item_images.external_url',           external_url          from public.goods_item_images      where external_url     is not null
  union all select 'community_posts.images',    jsonb_array_elements_text(images)     from public.community_posts        where jsonb_typeof(images) = 'array'
  union all select 'post_appeals.proof_images', jsonb_array_elements_text(proof_images) from public.post_appeals         where jsonb_typeof(proof_images) = 'array'
  union all select 'contact_messages.attachment_urls', unnest(attachment_urls)        from public.contact_messages       where attachment_urls  is not null
),
parsed as (
  select src, u, btrim(split_part(u, '?', 1)) as no_qs from url_refs
),
classified as (
  select src, u, no_qs,
    case
      when no_qs ~ '/storage/v1/object/(public|sign|authenticated)/'
        then regexp_replace(no_qs, '^.*/storage/v1/object/(?:public|sign|authenticated)/', '')
      when no_qs ~ '/storage/v1/render/image/(public|sign|authenticated)/'
        then regexp_replace(no_qs, '^.*/storage/v1/render/image/(?:public|sign|authenticated)/', '')
      when no_qs ~* '^https?://' then null
      else ltrim(no_qs, '/')
    end as bucket_path
  from parsed
),
url_pairs as (
  select split_part(bucket_path, '/', 1) as bucket,
         substring(bucket_path from position('/' in bucket_path) + 1) as path
  from classified
  where bucket_path is not null and position('/' in bucket_path) > 1
),
path_pairs as (
            select 'verify-documents' as bucket, ltrim(btrim(evidence_url), '/') as path
              from public.shop_verify_requests where evidence_url is not null
  union all select bucket_name, ltrim(btrim(object_path), '/')
              from public.goods_item_images where object_path is not null and bucket_name is not null
  union all select 'exhibit-images', ltrim(btrim(object_path), '/')
              from public.exhibit_images where object_path is not null
  union all select bucket_id, ltrim(btrim(object_path), '/')
              from public.exhibit_storage_cleanup_queue where object_path is not null
),
all_refs as (
  select bucket, path from url_pairs
  union all
  select bucket, path from path_pairs
)
select o.bucket_id                   as bucket,
       o.name                        as path,
       (o.metadata->>'size')::bigint as bytes,
       o.created_at,
       o.last_accessed_at,
       case
         when o.bucket_id = 'verify-documents'                      then '보류 — 인증 증빙'
         when o.name like 'dopaminseuteisyeon-yongsan-aipakeumol/%' then '보류 — shop_images 행 0'
         else '삭제 후보'
       end                           as 판정
from storage.objects o
where not exists (
        select 1 from all_refs a where a.bucket = o.bucket_id and a.path = o.name
      )
order by 판정, o.bucket_id, (o.metadata->>'size')::bigint desc;
```

### (2) 파싱 불가·외부 URL 분류

(1) 의 `url_refs` CTE 를 그대로 쓰고 아래로 집계한다.

```sql
select src as 출처,
       case
         when btrim(split_part(u,'?',1)) ~ '/storage/v1/(object|render/image)/' then '정상 파싱'
         when btrim(split_part(u,'?',1)) ~* '^https?://'                        then '외부 URL (Storage 아님)'
         when position('/' in ltrim(btrim(split_part(u,'?',1)), '/')) > 1       then 'path 형태'
         else '파싱 불가'
       end as 분류,
       count(*) as 건수,
       min(u) as 예시
from url_refs
group by 1, 2
order by 2, 3 desc;
```

### (3) URL 인코딩 경로 존재 여부

```sql
select count(*) filter (where name like '%\%%') as 인코딩_의심_오브젝트,
       count(*) as 전체
from storage.objects;
```

### (4) 도파민스테이션 용산점 판단 자료

```sql
select o.name as 경로,
       pg_size_pretty((o.metadata->>'size')::bigint) as 크기,
       o.metadata->>'mimetype' as 타입,
       o.created_at, o.updated_at, o.last_accessed_at,
       o.owner_id as 업로더,
       (select p.nickname from public.profiles p where p.id = o.owner_id) as 업로더닉네임
from storage.objects o
where o.bucket_id = 'shop-images'
  and o.name like 'dopaminseuteisyeon-yongsan-aipakeumol/%'
order by o.created_at;
```

```sql
select s.id, s.slug, s.name, s.status, s.added_by, s.owner_id, s.created_at,
       (select count(*) from public.shop_images si where si.shop_id = s.id) as 사진행수,
       (select count(*) from public.shop_highlights sh where sh.shop_id = s.id) as 하이라이트행수
from public.shops s
where s.slug like 'dopaminseuteisyeon%';
```

이미지 자체는 눈으로 봐야 판단이 된다. 두 파일의 공개 URL 을 받아서 확인할 것.

### (5) post-images 전수 참조 검사 (2단계)

먼저 검사 쿼리를 생성하고, 출력된 SQL 을 다시 실행한다.

```sql
select 'select * from (' || string_agg(
         format('select %L as 위치, count(*) as 건수 from public.%I where %I::text like ''%%post-images%%''',
                table_name || '.' || column_name, table_name, column_name),
         E'\n  union all ')
       || E'\n) t where 건수 > 0 order by 건수 desc;' as 실행할_쿼리
from information_schema.columns c
where c.table_schema = 'public'
  and c.data_type in ('text', 'character varying', 'jsonb', 'json', 'ARRAY')
  and exists (select 1 from information_schema.tables t
              where t.table_schema = 'public' and t.table_name = c.table_name
                and t.table_type = 'BASE TABLE');
```

0행이면 48개 전부 미참조 확정. 행이 나오면 그 컬럼을 참조 목록에 넣고 다시 계산한다.

## B-8. 이후 보고할 것

1. 정규화 기준 최종 대상 수와 예상 회수 용량
2. 파싱 불가 목록
3. 도파민스테이션 2장 판단 자료
4. post-images 전수 검사 결과
5. 백업 계획 확정 → 사용자 승인 → dry-run → 20개 배치 → 나머지

## B-9. 재발 방지 설계 (조사 보고만, 구현 금지)

사용자가 우선순위대로 요청한 항목이다. **아직 구현하지 말고 조사·설계 보고만 할 것.**

1. 샵 이미지 업로드 전 리사이즈 및 WebP 변환 — 장변 1600px + 품질 0.82 기준이면 7~9 MB → 200~400 KB (약 95% 감소). `goodsService.ts:401-407` 이 이미 `.webp` 로 올리고 있으니 그 방식을 참고
2. 허용 해상도·파일 크기·품질 기준 정의
3. 교체 성공 후 이전 오브젝트 삭제
4. DB 갱신 실패 시 새로 올린 오브젝트 롤백
5. 삭제 시 DB 행과 Storage 파일의 정합성
6. places·hero·review·community 에도 공통 적용 가능한지
7. 인증 증빙 보존 기간과 승인·거절·재신청별 삭제 정책 (`migrations/shop_verify_requests_cleanup.sql:31` 이 DELETE 권한을 회수해 구조적으로 영구 적재됨)
8. `routes/{userId}/new/` 경로 버그 — `RouteBuilder.tsx:116` 이 `routeKey='new'` 로 올림
9. 주기적인 고아 파일 탐지 dry-run 작업

**목표는 0.93 GiB 보다 더 여유 있게 낮추는 것이지만, 용량 확보를 위해 불확실한 파일을 삭제하지 않는다.**

---

# C. 새 대화 첫 메시지 예시

```
이 문서 그대로 이어서 진행해줘.
Storage 정리(B)부터. B-7 의 쿼리부터 순서대로 주면 돼.
작업 방식은 0번 규칙 그대로 지켜줘.
```
