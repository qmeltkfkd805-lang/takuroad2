# TAKUROAD 작업 인수인계 — 2026-09-23

새 대화에서 이 문서를 첨부하고 "이어서 진행해줘" 라고 하면 그대로 이어집니다.

**오늘 한 일은 전부 끝났고 푸시됐습니다.** 남은 것은 아래 "다음에 할 일" 뿐입니다.
이전 문서(`HANDOVER-2026-09-10.md`)는 A·B 섹션이 모두 완료 처리됐습니다 — 배경이 필요할 때만 보세요.

---

# 0. 작업 방식 (이걸 먼저 읽어주세요)

## 진행 방식

- **한 번에 한 단계씩.** 메시지 하나에 실행할 블록 하나를 주고, 결과를 받은 뒤 다음으로 갑니다
- 추측으로 넘어가지 않습니다. 컬럼 이름·FK 대상·기본값이 필요하면 **먼저 조회해서 확인**합니다
- **숫자가 같다고 통과가 아닙니다.** 오늘만 세 번 걸렸습니다 —
  참조 키 1,318 = 1,318인데 지문이 달랐고, 크기 검사 테스트가 실제로는 개수 검사에 걸렸고,
  lint 전후 총계가 같았는데 한쪽이 파싱 실패였습니다. 통과 판정 전에 **무엇이 실제로 실행됐는지** 확인하세요
- 제가 만든 것에서 결함을 찾으면 숨기지 말고 그 자리에서 말합니다

## SQL

- **채팅에 전문을 붙입니다.** 사장님 쪽에서 첨부 파일이 안 열립니다 (실행 도구 오류)
- 200줄이 넘으면 함수·블록 단위로 나눠서 순서대로 줍니다
- 적용 전에 사장님이 확인합니다. **"실제 적용은 제가 SQL을 확인한 뒤 진행해주세요"**
- **롤백 SQL 을 적용 블록과 같은 메시지에 넣지 않습니다** (예전에 롤백을 실수로 실행한 적 있음)
- Supabase SQL Editor 는 **선택한 부분만 실행하고 마지막 결과만** 보여줍니다.
  `begin; ... rollback;` 블록은 통째로 실행해야 합니다

## 파일 전달

- 파일 첨부나 다운로드 방식은 쓰지 않습니다
- 저장소 파일 수정은 **`device_commit_files`** 로 직접 씁니다 (이 세션에서 잘 동작했음)
- 부분 수정은 **PowerShell `.Replace` 블록**으로 줍니다. 다만 아래 두 가지를 반드시 지킵니다

### ⚠️ PowerShell 함정 두 개 (오늘 둘 다 걸림)

**1. `.NET` 메서드는 PowerShell 의 현재 위치를 안 따릅니다**

```powershell
# 틀림 — C:\Users\User\src\... 를 찾는다
$f = "src\components\X.tsx"
# 맞음
$f = "C:\dev\takuroad\src\components\X.tsx"
```

**2. 저장소 파일은 CRLF, here-string 은 LF 라 `Contains` 가 전부 실패합니다**

```powershell
$t = [System.IO.File]::ReadAllText($f)
$CR = [string][char]13
$LF = [string][char]10
$t = $t.Replace($CR + $LF, $LF)          # 파일을 LF 로 정규화

$old = @'
...
'@ -replace "`r`n", "`n"                  # here-string 도 LF 로

# 저장은 BOM 없는 UTF8. git 이 체크아웃 때 CRLF 로 되돌립니다
[System.IO.File]::WriteAllText($f, $t, (New-Object System.Text.UTF8Encoding $false))
```

**3. `git show | Out-File` 은 BOM·인코딩을 망가뜨립니다.** 변경 전 사본이 필요하면 `cmd` 리다이렉션:

```powershell
cmd /c "git show HEAD:src/x.ts > src\_before_x.ts"
```

## 검증

- **lint 는 줄 번호가 아니라 변경 전후 결과를 비교**합니다. 그리고 비교 전에 `Parsing error` 가 없는지 확인하세요 — 있으면 그 파일은 검사되지 않은 것이고 총계가 우연히 맞을 수 있습니다
- **권한 확인은 `has_table_privilege()`** 로 합니다. `information_schema` 를 `grantee in ('anon','authenticated')` 로 거르면 **PUBLIC 권한을 놓칩니다**
- **뷰는 소유자 권한으로 실행됩니다** (`security_invoker` 기본 off). 기반 테이블에 RLS 를 켜도 뷰 접근은 안 막힙니다. 뷰의 grant 를 따로 회수하세요
- **`pg_default_acl`** 때문에 새로 만든 테이블에 명시하지 않은 권한이 붙습니다. 오늘도 `service_role` 에 DELETE 가 자동으로 붙어 있었습니다

## 코드·디자인 규칙

- **이모지 사용 안 함.** 핑크는 활성 메뉴와 주요 등록 버튼에만
- 시안의 숫자·데이터를 하드코딩하지 않음. 기능 없는 장식용 버튼·필터 만들지 않음
- 기존 데이터 구조·서비스·권한·탭 전환 방식과 동작 유지
- 관련 없는 파일과 기존 변경 사항은 수정하거나 되돌리지 않음
- 새 UI 요소는 **CSS 모듈 키에 의존하지 말고 인라인 스타일**로 (키가 없으면 조용히 스타일이 빠짐)

## 커밋

- **커밋은 코드 변경 내용만 기록합니다.** 용량 수치나 "고아 0건" 같은 **측정 결과는 넣지 않습니다** — 측정 시점과 조사 범위가 있어야 의미가 있습니다
- 한국어로 "무엇을 왜". 끝에 아래를 붙입니다

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: <세션 URL>
```

- 커밋 메시지는 파일로 써서 `-F` 로 넘깁니다 (한글 인코딩)

```powershell
$p = "$env:TEMP\takuroad-commit-msg.txt"
[System.IO.File]::WriteAllText($p, $msg, (New-Object System.Text.UTF8Encoding $false))
git commit -F $p
Remove-Item $p
```

- `git add` 는 **경로를 명시**합니다. 작업 트리에 미추적 `src/scripts/**` 가 늘 있는데 그건 다른 worktree 의 이벤트 import 스크립트이고 커밋 대상이 아닙니다

## 환경

- Windows + PowerShell 5.1, 저장소 `C:\dev\takuroad`
- Next.js 16.2.9 App Router. `npm run dev` / `npm run build` 는 `--webpack` 을 씁니다.
  **맨 `npx next build` 는 Turbopack 을 써서 `.next` 를 망가뜨립니다**
- `Database` 타입이 `any` 라 새 테이블·RPC 를 추가해도 `tsc` 가 안 막습니다 (백로그 항목)

---

# A. 오늘 완료한 작업

## A-1. 커밋 목록 (전부 origin/main 반영됨)

| 커밋 | 내용 |
|---|---|
| `e92b1f4` | Storage 삭제 후보 탐지 v1 (SQL 뷰 + 함수) |
| `ecca339` | 관리자 대시보드 `StorageSection` 카드 |
| `757e0b7` | 핸드오버 문서의 낡은 진행 상태 정정 |
| `7a761ba` | **삭제 후보 탐지의 참조 목록을 `REF_SOURCES` 로 일원화** (v1 제거) |
| `4968c4b` | 문의 첨부 서버 승인 — 스키마와 RPC |
| `09a8657` | 문의 첨부 서버 승인 — 라우트·서비스·폼 |
| `2252492` | 미제출 첨부를 기존 정리 큐로 회수 |

## A-2. Storage 삭제 후보 탐지

**지금 구조** — 참조 지식은 TS 한 곳에만 있습니다.

| 역할 | 위치 |
|---|---|
| 참조 출처 정의 | `src/lib/storage/canonicalRef.ts` 의 **`REF_SOURCES`** (51개) |
| URL·경로 파싱, 불완전 사유 | 같은 파일의 `parseUrlRef` · `parsePathRef` · `parseUrlOrPathRef` |
| 참조 집합 | `buildReferenceSnapshot()` — **조회 실패 시 throw** |
| 객체 나열 | `storage_object_inventory(offset, limit, max_rows)` — 참조 판정 안 함 |
| 대조·판정 | `GET /api/admin/storage-orphans` |
| 화면 | 관리자 대시보드 `StorageSection` — 버튼을 눌러야 검사 |

**⭐ 새 업로드 경로가 생기면 `REF_SOURCES` 에 추가하세요.** 빠뜨리면 그 파일이 삭제 후보로 잘못 잡힙니다.

**v1 이 왜 틀렸는지** (`migrations/storage_orphan_detection_v2.sql` 에 전문 기록)

- `shop_images` 의 버킷을 상수로 박음 → `REF_SOURCES` 는 `storage_bucket` 컬럼을 읽음
- `evidence_url` 을 경로 전용으로 다룸 → 실제는 `urlOrPath`
- 외부 링크로 보인다고 컬럼 16개를 임의 제외
- 퍼센트 인코딩 미디코드 (`demon-slayer%20(1).png` ≠ `demon-slayer (1).png`)
- 정리 큐를 참조원에 포함 → **순환**. 큐는 삭제 예정 목록이지 사용 목록이 아니고, `cleanupWorker` 가 그 큐 항목의 참조 여부를 `REF_SOURCES` 로 판정하므로 아무것도 삭제되지 않음

**마지막 검사 결과 (2026-09-23)**: 1,190개 / 375.8 MB, 삭제 후보 0, `complete: true`, 출처 51개, 참조 키 1,318.
`event-goods` 가 323 MB 로 전체의 86% — 다음에 용량 문제가 생기면 볼 곳입니다.

## A-3. 문의 첨부 서버 승인 업로드

**왜 했나**: `contact-files` 버킷의 INSERT 정책이 `{public}` 에 조건이 버킷 이름뿐이었습니다.
비로그인 상태에서 anon 키만으로 아무 파일이나 올릴 수 있었고, 크기·형식 제한도 없었으며
DELETE 정책이 없어 지울 수도 없었습니다. 공개 버킷이라 올린 파일에 즉시 공개 URL 이 붙습니다.

**흐름**

```
파일 선택 → (압축) → POST /api/contact/attachments   서버가 초안·슬롯·경로 결정 + 서명 URL 발급
          → 브라우저가 서명 URL 로 직접 전송          바이트는 API 를 거치지 않음
          → POST /api/contact                        객체 존재·실제 크기 확인 후 접수
```

**DB**

| 객체 | 역할 |
|---|---|
| `contact_drafts` | 소유자·`open/submitted/expired`·만료 1시간·`reserved_count`(누적 5개 강제) |
| `contact_attachments` | 경로는 서버가 정함. `declared_size`(신고)와 `actual_size`(실측) 분리. 만료 행은 남김 |
| `contact_attachments_protected` | 보호 뷰 — 유효 예약 + 제출된 첨부 + 만료됐지만 정리 시점 전 |
| `contact_open_draft` / `contact_reserve_attachment` / `contact_resign_attachment` / `contact_submit_draft` / `contact_expire_drafts` | 전부 초안 행을 `for update` 로 잠그고 상태 재확인 후 진행 |
| `contact_enqueue_expired_attachments` / `contact_sync_cleanup_state` | 정리 큐 적재·결과 반영 |

전부 `service_role` 전용. 테이블·뷰는 RLS 를 켜고 정책을 만들지 않아 브라우저에서 완전 차단.
추적 행 보호를 위해 `service_role` 의 DELETE 도 회수했습니다.

**⚠️ 서명 업로드 URL 은 2시간이고 조정할 수 없습니다**
([Supabase 문서](https://supabase.com/docs/reference/javascript/storage-from-createsigneduploadurl)).
초안 만료(1시간)와 별개라 `cleanup_eligible_at = signed_at + 2시간 15분` 을 따로 둡니다.
그 전에 지우면 늦게 업로드된 객체가 다시 고아가 됩니다.
초안 생성 기준으로는 2시간 15분~3시간 15분 사이이고 항상 3시간 15분이 아닙니다.

**보장 범위**: 초안당 최대 5개 · 파일당 10 MiB · 미제출 첨부 정리.
**총사용량은 제한되지 않습니다** — 초안을 계속 새로 만들면 상한이 없습니다.

**검증 완료**

- RPC 단위: 예약 6번째 거부(22023) / 남의 초안 거부(42501) / 만료 초안 거부 / 10 MiB 경계 / `../../evil.sh` 확장자 미부착 / 제출 멱등성(문의 1건)
- 실제 폼: 첨부 2개 → 슬롯 1·2, `attached`, `actual_size` 실측값, `image/webp`(압축 적용)
- 정책 제거 후: 브라우저 직접 INSERT **42501 차단**, 서명 업로드 **성공**

**아직 안 돌려본 것**: 정리 동선. 보호 구간이 2시간 15분이라 만료 대상이 없었습니다.
크론 응답의 `attachmentsEnqueued`·`contactCleanupDone` 으로 확인하세요.

---

# B. 다음에 할 일

## B-1. 우선순위 높음

**1. `contact_files_read` — 첨부 읽기가 여전히 공개**
URL 을 알면 누구나 문의 첨부를 열 수 있습니다. 버킷을 private 로 돌리고 서명 URL 로 바꾸는 작업인데,
`contact_messages.attachment_urls` 에 저장된 기존 공개 URL 이 죽으므로 읽는 곳 두 군데를 같이 고쳐야 합니다
(`ContactAdminTab.tsx:379` 관리자, `MyContacts.tsx:86` 본인).

**2. 사용자별 총량·빈도 제한**
초안 생성 빈도와 기간별 업로드 총량. `contact_attachments` 가 `user_id`·`reserved_at`·`actual_size` 를
들고 있어 붙일 자리는 있습니다.

**3. `shops` 권한**
`owner_id` 가 authenticated INSERT 허용 컬럼에 있습니다.
`shops_update_tiered` 가 미청구 샵 전체를 아무나 수정 가능하게 합니다 (위키형 의도로 보이지만
`review_status`·`reviewed_at`·`reviewed_by` 가 트리거로만 막힘).

## B-2. 확인 필요 (이전 문서 기록이라 상태가 바뀌었을 수 있음)

- 이미지 교체 시 구 객체 삭제 / DB 갱신 실패 시 롤백 / 삭제 정합성
- 인증 증빙 보존 기간 정책 — `shop_verify_requests` DELETE 권한이 회수돼 구조적으로 영구 적재됨
- `routes/{userId}/new/` 경로 버그 (`RouteBuilder.tsx:116`)
- `run_fanart_selection` 의 `search_path`

## B-3. 코드 품질

- `export type Database = any` — lint 오류 약 1,280건
- `getAdminStats` 가 실패 시 0 반환 (오늘 만든 카드가 "0건 vs 검사 불완전" 을 구분한 것과 같은 문제)
- `NOTI_ICON` 맵이 `TopBar.tsx`·`NotificationsPage.tsx` 두 곳에 중복
- `<img>` → `next/image`

## B-4. 별도 확인 대상

**배지 아이콘 3개가 깨져 있습니다.** `badges.icon_url` 이 존재하지 않는 객체를 가리킵니다.

| 배지 | icon_url 파일명 |
|---|---|
| 귀멸의 칼날 | `demon-slayer%20(1).png` |
| 블루아카이브 | `blue-archive-logo.png` |
| 원피스 | `one-piece-cropped.png` |

셋 다 `shop-images` 버킷에 없습니다 (이름 일부로 넓게 검색해도 0행).

**증빙 URL 없는 인증 신청 2건** — `HANDOVER-2026-09-10.md` B-10 참고.
승인 상태는 그대로 둡니다.

**`selectAll` 중복** — `exhibit/cleanup` 라우트와 `storage-orphans` 라우트에 같은 페이지네이션 코드가
두 번 있습니다. 공용으로 빼려면 크론 경로를 건드려야 해서 이번엔 주석으로 출처만 표시했습니다.

---

# C. 새 대화 첫 메시지 예시

```
이 문서 그대로 이어서 진행해줘.
0번 작업 방식 먼저 읽고, B-1 의 1번(contact_files_read 읽기 좁히기)부터 하자.
```

또는

```
이 문서 이어서. 오늘은 B-3 코드 품질 중에 Database = any 부터 보자.
```
