import{createClient}from'@supabase/supabase-js';import{config}from'dotenv';config({path:'../.env.local'});const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);const r=await db.from('places').upsert({slug:'cgv-yeonnam',name:'CGV 연남',place_type:'CULTURE_SPACE',addr:'서울 마포구 양화로 186',region:'서울',district:'마포구',lat:37.5576757655085,lng:126.925869247772,kakao_place_id:'1066689336',category_name:'문화,예술 > 영화,영상 > 영화관 > CGV',parking:true,parking_note:'주차 가능(유료)\n당일 관람 티켓 인증 시 주중·주말 3시간 5,000원\n3시간 초과 시 15분당 1,500원\n차량 1대당 최대 3시간까지만 할인 적용'},{onConflict:'kakao_place_id'}).select('*').single();if(r.error)throw r.error;console.log(JSON.stringify(r.data,null,2))
