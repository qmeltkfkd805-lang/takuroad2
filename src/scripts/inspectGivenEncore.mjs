import{createClient}from'@supabase/supabase-js';import{config}from'dotenv';config({path:'../.env.local'});const d=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);const[e,t]=await Promise.all([d.from('events').select('id,title,start_date,end_date').or('title.ilike.%기븐%,title.ilike.%given exhibition%'),d.from('tags').select('*').or('name.eq.기븐,english_name.ilike.%given%')]);for(const r of[e,t])if(r.error)throw r.error;console.log(JSON.stringify({events:e.data,tags:t.data},null,2))
